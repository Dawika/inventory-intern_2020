(function () {
  'use strict';
  angular.module('somsri.main.help', [
  ])
    .controller('helpCtrl', ['$rootScope', '$scope', '$uibModal', '$window', 'translateFilter', function ($rootScope, $scope, $uibModal, $window, translateFilter) {
      $rootScope.menu = translateFilter('help')


    }]);
})();
