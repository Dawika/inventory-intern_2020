(function () {
  'use strict';

  angular
    .module('somsri.payroll.home', [])
    .controller('homeCtrl', homeCtrl);

  homeCtrl.$inject = ['$rootScope', 'translateFilter']

  function homeCtrl ($rootScope, translateFilter) {
    $rootScope.menu = I18n.t('payroll');
    $rootScope.loadAndAuthorizeResource("employee_me", function(){ 
    	// $rootScope.openMyProfile()
  	})
  }
})();
