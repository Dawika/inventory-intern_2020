(function() {
  'use strict';
  angular.module('somsri.invoice.menu', [
  ])
  .controller('menuCtrl', ['$rootScope', 'translateFilter', 'menuService', '$scope', function ($rootScope, translateFilter, service, $scope) {
    $rootScope.menu = I18n.t('invoices');
      $scope.isAdmin = false
      service.getPermission().then(function(resp){
        $scope.isAdmin = resp.data.permission
      })
  }]);
})();
